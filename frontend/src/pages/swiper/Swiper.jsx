import React from 'react'

const Swiper = () => {
  return (
    <div>
        const slides = [mahmutorhan, formula1, orchestra, justintimberlake, clon];

{slides.map((image, idx) => (
  <SwiperSlide key={image}>
    <img src={image} alt="" style={slideStyle} className="concertimage" />
  </SwiperSlide>
))}

    </div>
  )
}

export default Swiper